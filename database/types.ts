/**
 * The Kysely database schema.
 *
 * Each table gets its own interface, registered here as migrations
 * introduce it. Kysely uses this type to make every query type-safe.
 */
export interface Database {}
